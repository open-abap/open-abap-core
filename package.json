{
  "name": "open-abap",
  "version": "1.0.0",
  "private": true,
  "description": "test",
  "main": "index.js",
  "scripts": {
    "lint": "abaplint",
    "unit": "rm -rf output && abap_transpile && echo RUNNING && node output/index.mjs",
    "test": "npm run lint && npm run unit"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@abaplint/cli": "^2.78.12",
    "@abaplint/runtime": "^1.6.27",
    "@abaplint/transpiler-cli": "^1.6.27"
  }
}
